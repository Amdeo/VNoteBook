vnote_backup_file_826537664 G:/jianguo/我的坚果云/code/VNoteBook/代码片段/C++/文件操作.md
# 文件操作
## fget

```C++
#include <stdio.h>
char *fgets(char *str, int n, FILE *stream);
```

使用 
```C++
char a[10];
fgets(a,10,stdin) # 获取键盘输入
fgets(a,10,stdout) # 获取终端输出内容
fgets(a,10,fp) # 获取文件内容
```
